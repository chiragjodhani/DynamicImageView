# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DynamicImageChanges' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DynamicImageChanges
  pod "MMSProfileImagePicker"
  pod 'PhotoEditorSDK', '~> 7.2'
  pod 'LSDialogViewController'
  pod 'PhotoTweaks'
  target 'DynamicImageChangesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DynamicImageChangesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
